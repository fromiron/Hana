//global
const HOME = '/';

const ABOUT_US = '/about';

const SERVICE = '/service';

const PLAN = '/plan';

const CONTACT_US = '/contact';

const MAIL = '/mail';

const routes = {
  home: HOME,
  about: ABOUT_US,
  plan: PLAN,
  service:SERVICE,
  contact: CONTACT_US,
  mail:MAIL,
};

export default routes;
